---
- hosts: all

  roles:
    - common
    - {role: offline, when: offline}
    - stow
    - ncurses
    - tmux
    - git
    - vim
    - zsh
    - htop
    - ldconfig
    - dotfiles
    - {role: package_env, when: create_package}
