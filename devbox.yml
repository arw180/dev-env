---
- hosts: all

  roles:
    - {role: offline, when: offline}
    - common
    - stow
    - ncurses
    - tmux
    - git
    - dotfiles
    - vim
    - zsh
    - htop
    - ldconfig
    - {role: package_env, when: create_package}
